apiVersion: v1
kind: Service
metadata:
  name: {{ include "peerbox.fullname" . }}
  labels:
    {{- include "peerbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "peerbox.selectorLabels" . | nindent 4 }}
