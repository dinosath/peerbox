{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "peerbox.serviceAccountName" . }}
  labels:
    {{- include "peerbox.labels" . | nindent 4 }}
{{- end }}
